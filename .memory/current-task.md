---
name: Current Task
description: User requested to create a specific set of tasks with defined dependencies for a development workflow.
type: project
---

Create tasks for: setup database schema, create API endpoints (depends on schema), write tests (depends on endpoints), write docs (depends on schema).
